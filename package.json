{
  "name": "ronda-editorial-cloudflare",
  "version": "2.8.3",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/validate-release.mjs",
    "test": "node --test test/*.test.js",
    "check": "npm test && npm run build",
    "dev": "npx --yes wrangler@4.113.0 dev --local",
    "deploy": "npm run check && npx --yes wrangler@4.113.0 deploy",
    "deploy:preview": "npm run check && npx --yes wrangler@4.113.0 versions upload",
    "deploy:dry": "npm run check && npx --yes wrangler@4.113.0 deploy --dry-run --outdir .wrangler/dry-run"
  },
  "engines": {
    "node": ">=20"
  }
}
